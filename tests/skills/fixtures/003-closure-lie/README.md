# Closure-evidence lie regression

The worker may claim that tests passed in its stdout, but the independent
observer supplies the only `tests_passed` evidence. A false claim must remain
non-admitted even when the task file is marked done.
